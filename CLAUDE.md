@AGENTS.md

# Git — Atualização Automática

Após concluir qualquer conjunto de mudanças neste projeto, **sempre** faça commit e push para o repositório remoto:

```
git add <arquivos modificados>
git commit -m "<mensagem descritiva>"
git push origin main
```

- Use mensagens de commit em português, descritivas do que foi feito
- Adicione apenas os arquivos modificados (nunca `git add -A` sem revisar)
- Nunca force-push em `main`
- Sempre faça push ao final de cada tarefa concluída

# Versão do Sistema

A versão fica em `src/lib/version.ts` (constante `APP_VERSION`) e exibida no rodapé do sidebar.

**Regra:** a cada conjunto de mudanças commitado, incremente a versão seguindo semver:
- `patch` (x.x.**N**) — correções de bugs, ajustes visuais, pequenas melhorias
- `minor` (x.**N**.0) — funcionalidades novas ou mudanças relevantes de UX
- `major` (**N**.0.0) — mudanças arquiteturais grandes (raramente)

Atualize `src/lib/version.ts` junto com os demais arquivos no mesmo commit.

# Projeto — Universidade LV

**Criador:** Thiago Leal da Silva

Plataforma de ensino para agentes de viagem. Next.js App Router + Supabase.

## Stack
- **Frontend**: Next.js App Router, TypeScript, Tailwind CSS, shadcn/ui, Lucide icons, Sonner (toasts)
- **Backend**: Supabase (Postgres + Storage + Auth)
- **Tabelas principais**: `profiles`, `training_items`, `training_materials`, `notifications`, `announcements`, `site_settings`, `lessons`, `modules`, `courses`

## Supabase
- `createClient()` — cliente com sessão do usuário (respeita RLS)
- `createAdminClient()` — usa `SUPABASE_SERVICE_ROLE_KEY`, bypassa RLS; use para operações admin
- Projeto ID: definido em `NEXT_PUBLIC_SUPABASE_URL` (ver `.env.local`)
- RLS `profiles`: usuário lê/edita apenas o próprio; admins leem todos (`profiles_select_admin`)
- Notificações: INSERT via `adminClient` (sem política de INSERT — service role bypassa RLS)
- **Relações N→1** (ex.: `tags(name)`, `courses(name)`, `lessons(title)` em joins): o PostgREST retorna **objeto** em runtime, mas o client sem tipos gerados infere **array**. Nunca acesse com `.map()`/`[0]` direto — use `toOne()` de `src/lib/supabase/relations.ts`, que normaliza as duas formas. (Regressão real na v1.50.1: Marketing quebrou com `t.tags.map is not a function` só para usuários com tags.)
- Triggers em `profiles` disparados via auth (GoTrue) rodam com `search_path` restrito — sempre qualifique objetos com `public.` dentro de funções de trigger (ver migração 030).
- **RLS bloqueia joins embutidos para linhas de outro dono, silenciosamente** (sem erro, só vem `null`/vazio): se a query do MEMBRO (client de sessão) faz embed tipo `profiles!fk(full_name)` para um `id` que não é o dele mesmo (ex.: nome do admin responsável por um chamado), a RLS de `profiles` (só lê o próprio perfil) barra o join e o campo volta vazio — mesmo a linha principal sendo do próprio membro. Real em `getMyFeedbackReports` (`src/app/actions/feedback.ts`): resolvido trocando para `adminClient` com filtro explícito `.eq('user_id', user.id)` (a checagem de posse fica no filtro, não na RLS).
- Pacotes `@tiptap/*` precisam ficar todos na MESMA versão exata (ver `package.json`) — as extensões fixam peer dependency `@tiptap/core` na versão exata, não em range; um caret (`^`) num único pacote causa conflito de peer dependency no `npm install` assim que o tiptap publica um patch novo.
- `<Select>` (`src/components/ui/select.tsx`) é baseado em `@base-ui/react/select`, não Radix. **`<SelectValue />` sozinho renderiza o valor bruto**, não o label do `<SelectItem>` selecionado — ele não escaneia os itens renderizados. Precisa de `<SelectValue>{(v) => label[v]}</SelectValue>` (função de mapeamento) ou do prop `items` no `<Select>` raiz. Sem isso, o trigger mostra o `value` cru (ex.: um UUID) em vez do texto — bug real visto no seletor de "Responsável" em `feedback-panel.tsx`.
- **`toast()` (sonner) chamado no primeiro `useEffect` de montagem de uma página pode ser perdido silenciosamente.** O `<Toaster/>` (root layout) só recebe eventos publicados DEPOIS que seu próprio efeito de `ToastState.subscribe()` rodar — e esse subscribe não faz replay de toasts anteriores. Como `{children}` vem antes de `<Toaster/>` no JSX do `src/app/layout.tsx`, um `toast()` disparado no efeito de mount de uma página corre — e às vezes perde — a corrida contra o subscribe do Toaster (mesmo commit, ordem de efeitos por posição na árvore). Sintoma: nenhum erro, o toast simplesmente não aparece, um clique/ação do usuário depois disparando outro toast funciona normal (não é o Toaster que está quebrado, só a chamada síncrona no mount). Correção: empurrar a chamada com `setTimeout(() => toast(...), 0)`. Caso real: toast de "desconectado por inatividade" em `src/components/auth/login-form.tsx` após redirect de `idle-logout-guard.tsx`.

## Papéis e autorização (admin / collaborator / member)
- `profiles.role` aceita `'admin' | 'member' | 'collaborator'` (migração `036`). **Colaborador** = pessoa interna que cria/edita conteúdo apenas da sua área, e estuda como membro comum no `/dashboard` (sem preview de rascunho).
- **Áreas de colaborador** são configuráveis pelo admin (tabela `collaborator_areas`: `name` + `capabilities text[]`), geridas em Admin → Membros → "Áreas de Colaborador" (`collaborator-areas-manager.tsx`). Cada colaborador pertence a UMA área (`profiles.collaborator_area_id`). **Decisão: NÃO reutilizar `tags`** — tags têm semântica de visibilidade de aluno; misturar criaria risco de apagar uma "tag-área" sem perceber que dava poder de escrita.
- **Cor de tag é hex livre desde v1.77.0** (`tags.color`, sempre foi `TEXT` sem CHECK, só a UI restringia). `src/lib/tag-colors.ts`: `getTagColor(color)` resolve tanto hex (`#rrggbb`) quanto as 10 chaves antigas (`resolveTagHex`, mapa de compat — tags criadas antes da v1.77 continuam funcionando sem migração de dado). Retorna `{ chipStyle, dotStyle, textStyle }` (inline `style`, não mais classes Tailwind — hex arbitrário em runtime não dá pra virar classe Tailwind). UI de escolha (`tags-manager.tsx`, componente `ColorPicker`): 10 bolinhas de atalho + `<input type="color">` nativo (paleta do SO) + campo de texto pro hex — os 3 escrevem no mesmo state. Ao adicionar um novo lugar que renderiza cor de tag, usar `getTagColor()` e aplicar via `style`, não `className`.
- **Capacidades** (enum fixo em `src/lib/capabilities.ts`): `courses` (cursos+módulos+aulas), `trainings`, `marketing`, `comercial`, `aereo`, `famtours`, `grupos`. Premiação (TamoJunto Winners), PodViajar e Corrida de Vendas gravam settings globais (`site_settings`, sem `owner_area_id` — não fazem sentido "por posse"). **Desde v1.74.0, colaborador com a capacidade `marketing` também edita as 3** (antes eram admin-only puro, sem nenhuma capacidade cobrindo). `src/app/actions/marketing-settings.ts` usa `requireCapability('marketing')` nos 3 (`saveTamojuntoWinners`/`savePodviajar`/`saveCorridaVendas`); os 3 managers (`tamojunto-winners-manager.tsx`/`podviajar-manager.tsx`/`corrida-vendas-manager.tsx`) recebem a prop `canEdit` (computada em `admin/marketing/page.tsx` como `isAdmin || ctx.capabilities.includes('marketing')`, repassada via `marketing-tabs.tsx`) em vez do antigo `isAdmin` — mesmo padrão `<fieldset disabled={!canEdit}>` já usado no resto do painel. **Aviso explícito quando `!canEdit` (v1.84.1)**: `tamojunto-winners-manager.tsx` e `podviajar-manager.tsx` mostram um banner âmbar ("Sua área não tem permissão para alterar... — fale com um admin") acima do `<fieldset>` — antes disso os controles só ficavam cinza/desabilitados sem explicar o motivo. `corrida-vendas-manager.tsx` não ganhou o mesmo aviso ainda (não foi pedido). **Área "Comercial Internacional" teve a capacidade `famtours` removida (v1.84.1, mudança de dado em `collaborator_areas`, não de código)** — a pedido do usuário; a área nunca teve `marketing`, então Premiação/PodViajar já estavam bloqueados pra ela antes disso, só faltava o aviso explicando por quê.
- **Dono do conteúdo**: `owner_area_id` (nullable, NULL = global/admin) em `courses`, `training_items`, `marketing_items`, `famtours`, `grupos`, `commercial_conditions`. `modules`/`lessons` herdam via `course_id`. **Reorder global (cursos/módulos/treinamentos) é admin-only** — reordenar um subconjunto bagunçaria os índices; reorder de aulas (escopado ao módulo) exige posse do curso pai.
- **Colaborador VÊ TUDO, só EDITA o que tem capacidade + posse** (v1.71.0 — antes era "só vê o que é seu"). `/admin/cursos` e `/admin/marketing` (as duas únicas telas de conteúdo) mostram TODOS os cursos/treinamentos/itens/famtours/grupos/condições de TODAS as áreas pra qualquer colaborador, inclusive abas sem nenhuma capacidade relacionada (Comercial, Aéreo, Grupos, Corrida de Vendas, Premiação, PodViajar aparecem sempre, em modo leitura). Abrir o detalhe de um curso/módulo/aula de outra área **não bloqueia mais** — abre normal, só os controles de mutação ficam escondidos/desabilitados. Cada tela calcula `canEdit`/`canCreate` (`ctx.role === 'admin' || (ctx.capabilities.includes(cap) && item.owner_area_id === ctx.areaId)`) e repassa como prop pros componentes — **isso é só UX**, a segurança real continua 100% nos guards de mutação (item abaixo), que não mudaram. Padrão de UI pro modo leitura: `<fieldset disabled={!canEdit}>` em volta de formulários inteiros (desabilita inputs/botões nativamente em cascata) + condicionais explícitas pra overlays de upload e pro toolbar do `RichTextEditor` (prop `editable`, não é controle de formulário nativo, fieldset não alcança). Em listas de item (famtours/grupos/etc, fora de um único form), cada botão de Editar/Excluir/Publicar fica atrás de `{item.canEdit && (...)}` explícito — o link de ABRIR o detalhe fica sempre visível (é a porta de entrada pro modo leitura).
- **Camadas de autorização** (`src/lib/authz.ts`): `getAdminContext()` monta `{role, areaId, capabilities}`; guards de action (`requireAdmin`, `requireCapability`, `requireContentAccess`, `requireCourseAccess`→`requireModuleAccess`→`requireLessonAccess` sobem a cadeia de posse) retornam `{error}` — **estes são a segurança real, intocados pela mudança de visibilidade acima**. Guards de página: `requireContentPage()` (novo — só exige admin/collaborator ativo, sem checar capacidade nem posse, usado como entrada em `cursos/page.tsx`/`marketing/page.tsx`) substituiu `requirePageCapability` nesse papel; `requireCoursePage`/`requireModulePage`/`requireLessonPage` agora só confirmam que a entidade existe (404-like), sem bloquear por posse. `requireAdminPage` (admin-only) e `requirePageCapability` (capacidade sem posse) continuam existindo para as demais telas. **As policies RLS continuam admin-only de propósito** — a mutação do colaborador passa pelo guard e usa `adminClient`; se alguma action esquecer o guard/adminClient, a RLS bloqueia silenciosamente (0 linhas, sem erro) — mesmo gotcha da nota de feedback acima.
- **TODA server action de mutação tem guard explícito no topo** (padrão `const ctx = await requireX(); if ('error' in ctx) return { error: ctx.error }`): conteúdo usa capacidade+posse; `members/tags/settings/marketing-settings/announcements/seo/faq` usam `requireAdmin()` (antes não tinham NENHUMA checagem — brecha fechada na v1.63.0). Ao criar action nova, sempre incluir guard — o gate do layout não protege actions.
- Entrada no `/admin`: `proxy.ts` e `admin/layout.tsx` aceitam admin e collaborator ativo. Telas de detalhe buscam dados via `adminClient` APÓS o guard de posse (client de sessão esconderia rascunhos do próprio colaborador).
- **Menu do colaborador é igual ao do admin, exceto Membros/SEO/FAQ/Comunicados** (v1.72.0; Comunicados removido do menu do colaborador na v1.84.0) — `admin/layout.tsx` monta `allowedHrefs` como lista fixa (não mais por capacidade). Cada tela aberta tem sua própria regra, sem capacidade nova: **Dashboard** e **Relatórios → Ensino/Marketing** mostram os mesmos dados de sempre, sem filtro (decisão do usuário: não é "conteúdo de posse"). **Configurações** e **Feedback**: se `ctx.role !== 'admin'`, a própria page.tsx renderiza o componente de MEMBRO (`PreferencesForm`/`FeedbackPageContent`, extraídos pra `src/components/members/` e reusados nas duas rotas — mesmo padrão de `ProfileTabs` já usado no Perfil) em vez do painel de gestão — Feedback continua exigindo a tag "Beta" igual a qualquer membro. **Comunicados**: **desatualizado, revertido na v1.84.0** — chegou a ser liberado pra qualquer colaborador (`requireAnyRole()`, removido) entre v1.72.0 e v1.84.0, mas o usuário decidiu reverter: comunicado vai pra TODOS os membros da plataforma, não faz sentido pra algo específico de curso/área — quem precisa avisar algo usa a Comunidade do próprio curso. `createAnnouncement` voltou a `requireAdmin()` + sempre cria como rascunho; página (`admin/comunicados/page.tsx`) voltou a `requireAdminPage()`; item removido de `allowedHrefs`; UI (`announcements-manager.tsx`) esconde "Novo Comunicado" quando `!isAdmin` (esse prop hoje só chega `true`, já que a página é admin-only, mas o componente continua aceitando o prop pra clareza). **Bug real corrigido de passagem**: o painel "Acesso Rápido" do Dashboard (`admin-dashboard-shell.tsx`, `QUICK_LINKS`) nunca respeitava `allowedHrefs` — colaborador via um atalho pra "Comunicados" (e também "Membros", já escondido do menu desde v1.72.0) que só redirecionava de volta ao clicar. `admin/page.tsx` agora computa a MESMA lista de `allowedHrefs` de `admin/layout.tsx` (duplicada, mesmo padrão já usado ali) e passa como prop nova pro shell, que filtra `QUICK_LINKS` por ela. **Documentos/Certificados**: `issueCertificate`/`approveCertificate`/`revokeCertificate` trocaram `requireAdmin()` por `requireModuleAccess(moduleId)` (reaproveita a capacidade `courses` — emitir certificado exige ser dono do curso do módulo); configuração global do certificado (template/assinatura/posição do nome) continua `requireAdmin()` e some da tela pro colaborador (`canManageSettings` em `certificates-admin.tsx`). **Comunidade**: guard de entrada mudou; moderação (pin/lock/hide) passou a ser por posse na v1.80.0 (ver seção "Notificações e moderação por posse" abaixo) — **desatualizado**: não é mais `isAdmin` puro. **Relatórios → Atividades**: filtrada por posse pra colaborador — `relatorios/page.tsx` calcula `{courseIds, moduleIds, lessonIds}` da própria área e `atividades-log.tsx` monta um `.or()` combinando `and(entity_type.eq.X,entity_id.in.(...))` pros 3 tipos; sem curso próprio, força lista vazia.
- **Bug real corrigido (v1.73.1): `FeedbackTicketForm` redirecionava sempre para `/dashboard/feedback` após enviar um chamado**, mesmo sendo compartilhado com `/admin/feedback` (colaborador). Um colaborador (ex.: Shayene, área Marketing) que abria um chamado dentro do painel admin era jogado pra área do aluno logo depois de enviar — `router.push` estava com o path hardcoded em vez de usar o `pathname` (já capturado via `usePathname()` para o campo `page_url`). Corrigido para `router.push(\`${pathname}?tab=minhas\`)`, que funciona nas duas rotas.
- **Bug real corrigido (v1.74.1): admin que clicava "Abrir meu chamado" (em `/admin/feedback`) era mandado pra `/dashboard/feedback`** — o `<Link>` do botão apontava direto pra área do aluno (`src/app/(admin)/admin/feedback/page.tsx`), então até o admin saía do próprio painel só pra abrir um chamado. Diferente do colaborador (que já tinha esse fluxo embutido desde v1.72.0, ver acima), o ramo `ctx.role === 'admin'` da página sempre renderizava só `FeedbackPanel` (fila de triagem), nunca `FeedbackPageContent` (form de abrir/ver chamados). Corrigido reaproveitando o mesmo query param `tab` já usado pro colaborador: `?tab=abrir`/`?tab=minhas` agora também funciona pra admin **na mesma rota** `/admin/feedback` (renderiza `FeedbackPageContent` com um link "Voltar para a fila de chamados" acima, em vez do painel de triagem) — o botão "Abrir meu chamado" só mudou o `href` de `/dashboard/feedback?tab=abrir` pra `/admin/feedback?tab=abrir`.
- `notifyAllMembers` e o `notifyMembers` de training incluem `role IN ('member','collaborator')` — colaboradores também estudam e recebem avisos.
- **Cadastro pendente — aprovar (com papel/tags/área/cursos num só passo) ou recusar (v1.78.0)**: `profiles.rejected_at` (migração `045`, nullable) marca um cadastro como recusado sem apagar a conta — `pending = !active && !rejected_at`, `rejected = !!rejected_at`, `active = active || role==='admin'` (`admin/membros/page.tsx`). `rejectMember(userId)` seta `rejected_at` + `logActivity` + e-mail `emailMemberRejected` (template `member_rejected`); `reconsiderMember(userId)` só limpa `rejected_at` + `logActivity`, sem e-mail. UI: `RejectButton` (AlertDialog de confirmação) ao lado de "Aprovar" em `pending-members.tsx`, e nova lista "Recusados" (`rejected-members.tsx`, botão "Reconsiderar" como rede de segurança) renderizada entre "Aguardando aprovação" e a tabela geral em `membros-tabs.tsx`. `ApproveDialog` deixou de ser só "aprovar com cursos" — agora configura **papel (member/collaborator/admin) + área do colaborador + tags** no mesmo diálogo (campos idênticos aos de `EditMemberDialog`, reaproveitando `getTagColor().chipStyle`), disparando `approveMember` + `updateMemberRole` + `assignMemberTags` em paralelo (`Promise.all`) — **os 3 retornos precisam ser checados** (`approveResult?.error || roleResult?.error || tagsResult?.error`), não só o do primeiro; checar só um deixaria os outros dois falharem em silêncio enquanto a UI mostra "Membro aprovado!" (bug real corrigido nesta versão). `EditMemberDialog` continua existindo pra promover/editar depois da aprovação (fluxo antigo intocado). **Cuidado ao testar isto via script/Playwright**: o botão fica em estado "Aprovando..." enquanto o `Promise.all` das 3 actions roda (cada uma faz 1-2 idas ao Supabase, incluindo o `logActivity` fire-and-forget) — fechar o browser ou consultar o banco antes do botão sair desse estado captura um resultado parcial que parece bug mas é só timing do teste, não da aplicação; esperar o texto "Aprovando..." sumir antes de verificar.
- **Bug real corrigido (v1.83.2): o redirect de `/api/admin/preview` mandava o navegador pra `localhost:3000` mesmo em produção** (`ERR_SSL_PROTOCOL_ERROR` — o navegador tentava HTTPS numa porta que só fala HTTP local). Causa: `new URL('/admin', request.url)` — atrás do proxy reverso da VPS, `request.url`/`request.nextUrl.origin` refletem o endereço INTERNO que o nginx repassa pro processo Next.js (`http://localhost:3000/...`), não o domínio público, porque o nginx não repassa `Host`/`X-Forwarded-*` corretamente. Corrigido reaproveitando o mesmo padrão de `siteUrl` já usado em `src/lib/email.ts`: `process.env.NEXT_PUBLIC_SITE_URL ?? 'https://universidadelv.com.br'` (constante local no route.ts, não um helper compartilhado) — **local precisa de `NEXT_PUBLIC_SITE_URL=http://localhost:3000` no `.env.local`** (adicionado), senão o redirect manda o dev local pra produção de verdade.
- **Modo prévia "ver como colaborador de uma área específica" (v1.79.0, reformulado na v1.83.0/v1.83.1)**: cookie `preview_collaborator` (`src/lib/preview.ts`, `PREVIEW_COOKIE`) guarda o **id da `collaborator_area`** escolhida (não mais um booleano `'1'`). O controle na Dashboard (`AdminDashboardShell`) é um `<select>` nativo listando `collaboratorAreas` (buscadas via `getCollaboratorAreas()` tanto em `admin/layout.tsx` quanto em `admin/page.tsx`, cada um por conta própria) — escolher uma área já ativa a prévia direto; ativo, vira botão "Sair da prévia (NomeDaÁrea)". Banner da sidebar (`AdminSidebar`) mostra o mesmo nome entre parênteses. **Ativar abre em ABA NOVA** (`window.open('/api/admin/preview?area=<id>', '_blank')`) — a aba original do admin não navega nem recarrega, continua 100% com o painel completo. **Sair da prévia** faz `window.location.href = '/api/admin/preview'` na própria aba da prévia. **`src/app/api/admin/preview/route.ts` (GET, não Server Action) é o mecanismo — de propósito**: setar/limpar o cookie via Server Action (como era na v1.83.0) faz o Next.js App Router revalidar automaticamente a página que CHAMOU a action, mesmo sem `revalidatePath`/`router.refresh()` explícito nela — isso quebrava o isolamento entre abas (a aba original também se atualizava sozinha assim que a promise da action resolvia, mesmo sem eu pedir). Uma navegação HTTP de verdade (GET + redirect) só afeta a aba que de fato navegou — por isso a troca de mecanismo. `getAdminContext().role === 'admin'` é checado dentro do route handler antes de mexer no cookie; sem token CSRF (aceitável — o único efeito de um GET forjado seria trocar a própria simulação de VIEW do admin, não uma mutação real).
- **`getPreviewAreaContext(ctx)` (`src/lib/authz.ts`)** é o mecanismo real por trás da simulação: recebe o `ctx` REAL (de `requireContentPage()`/`requireCoursePage()`/etc.) e devolve ou o mesmo `ctx` (sem prévia ativa, ou usuário já é colaborador de verdade) ou um `ctx` **sintético** de colaborador (`role:'collaborator'`, `areaId` = área escolhida, `capabilities` daquela área) quando a prévia está ativa. **Só usar o resultado pra decidir o que a TELA mostra** (`canEdit`/`canCreate`/`canModerate`/`isAdmin` de UI) — nunca dentro de guards de ACTION. Os 9 pontos que hoje calculam esse tipo de flag (`admin/cursos/page.tsx`, `admin/cursos/[id]/page.tsx`, `admin/modulos/page.tsx`, `admin/modulos/[id]/page.tsx`, `admin/aulas/[id]/page.tsx`, `admin/marketing/page.tsx`, `admin/comunidade/[courseId]/page.tsx` e `.../[postId]/page.tsx`) chamam `const viewCtx = await getPreviewAreaContext(ctx)` logo após o guard real e usam `viewCtx.role`/`viewCtx.areaId`/`viewCtx.capabilities` em vez de `ctx.*` só nesses cálculos — o `ctx` real continua sendo usado pra qualquer coisa que precise da identidade verdadeira (nenhuma, hoje, fora dos próprios guards). Em `marketing/page.tsx`, os `userRole`/`userAreaId`/`userCapabilities` repassados pra `MarketingTabs`→`MarketingManager` (que computam seu próprio `canEdit` mais abaixo na árvore) também viraram os de `viewCtx` — a simulação propaga sozinha sem precisar tocar esses componentes filhos.
- **Nenhum guard de mutação lê o cookie nem `getPreviewAreaContext`** — `requireCourseAccess`/`requireCapability`/etc. dentro das *actions* de mutação sempre usam `getAdminContext()` puro (papel real), então o admin sempre edita de verdade ao sair da prévia (ou poderia, via chamada direta da action, mesmo com a prévia ativa — só a UI é que fica com os campos desabilitados via `fieldset disabled={!canEdit}`, escondendo o caminho de clicar "Salvar", não bloqueando a mutação em si). Isso é deliberado: a prévia simula fielmente o que aquela área veria/editaria (inclusive escondendo criação de enquete na Comunidade, reorder de curso/módulo, etc. — tudo que já era `isAdmin`-gated na UI passou a usar `viewCtx.role === 'admin'`), mas nunca é a barreira de segurança real.

## Notificações e moderação de Comunidade por posse (v1.80.0)

- **`notifyCourseOwners(courseId, actorId, opts)`** (`src/app/actions/notifications.ts`) substitui `notifyAllAdmins` nos 3 pontos onde o evento é "sobre um curso específico": `task_submitted` (`lesson-tasks.ts`, resolve `courseId` a partir de `lessonId` via `lessons.module_id → modules.course_id`, usando `toOne()` pro embed N→1), `community_new_post` e `community_new_reply` (`community.ts`, `courseId` já vem como parâmetro da action). Resolve `courses.owner_area_id` → `collaborator_areas` (só entra se a área tiver a capacidade `courses`) → todos os `profiles` daquela área → insere notificação pra cada um. **Sempre** notifica os admins também (mesmo sem dono, ou se a área não tiver a capacidade) — a cópia do admin ganha `area_tag` = nome da área quando havia dono, `null` caso contrário. `new_member_pending` e `new_feedback`/`feedback_update` continuam 100% `notifyAllAdmins` (decisão do usuário: cadastro pendente e feedback ficam admin-only).
- **Coluna `notifications.area_tag`** (migração `046`, nullable) — só populada na cópia do admin. Renderizada como badge `🏷 {área}` em `notification-bell.tsx`, logo abaixo do título — é a marcação visual pra admin saber "isso veio de uma área de colaborador". Sem badge = conteúdo global ou colaborador sem capacidade `courses`.
- **Moderação de Comunidade por posse**: `togglePinPost`/`toggleLockPost`/`hidePost`/`hideReply`/`deletePost` (`community.ts`) trocaram `requireAdmin()` por `requireCourseAccess(courseId)` — admin sempre passa, colaborador só se tiver a capacidade `courses` E for dono daquele curso. **`deleteReply` continua de fora de propósito** (decisão da v1.70.0 preservada: o próprio autor também exclui a própria resposta; dar essa ação a um colaborador-dono via `canModerate` quebraria, já que o server não tem esse guard — ver nota abaixo).
- **UI: prop `canModerate` (default = `isAdmin`)** em `community-posts.tsx` e `community-post-view.tsx`, separada de `isAdmin` — `isAdmin` real continua controlando só a criação de enquete (admin-only, não é sobre moderação de posse); `canModerate` controla pin/lock/hide/mascaramento (`isMaskedForViewer`) e `canDelete` de **post**. **Exceção**: `canDelete` de **resposta** (`ReplyItem`) fica preso a `isAdmin || isAuthor` (não `canModerate`), justamente porque `deleteReply` não ganhou o guard de posse — se usasse `canModerate`, o botão apareceria pro colaborador dono mas a exclusão de resposta de terceiro falharia silenciosamente na RLS (mesmo gotcha de sempre).
- **Indicador "Nova atualização" no card do chamado (v1.80.1)**: `getUnreadFeedbackUpdateReportIds()`/`markFeedbackReportNotificationRead(reportId)` (`notifications.ts`) — parseiam/filtram pelo `report=<id>` já embutido no `link` das notificações `feedback_update` (v1.75.0), sem precisar de coluna nova. `my-feedback-list.tsx` busca a lista no mount (`useEffect`), mostra um badge "✨ Nova atualização" ao lado do título do card, e marca como lida (server + estado local) só o chamado que foi aberto — não usa `markNotificationsByTypeRead` (que marcaria TODOS os `feedback_update` do usuário, apagando o indicador dos outros chamados por engano).
- **`admin/comunidade/[courseId]/page.tsx` e `.../[postId]/page.tsx`** calculam `canModerate = ctx.role === 'admin' || (ctx.capabilities.includes('courses') && course.owner_area_id === ctx.areaId)` e passam como prop nova. **Bug real corrigido de passagem**: `[postId]/page.tsx` ainda usava `requireAdminPage()` (admin-only) — um colaborador dono do curso conseguia ver a lista de posts (página pai já usava `requireContentPage()` desde a v1.72.0) mas era redirecionado ao tentar abrir um post específico. Trocado para `requireContentPage()` também.

## Logout automático por inatividade (só área do aluno)
- Escopo é **só `/dashboard`** (membro/colaborador estudando) — `/admin` não tem esse guard, de propósito (decisão do usuário).
- `src/components/ui/idle-logout-guard.tsx` (montado uma vez em `dashboard/layout.tsx`): timer único (`setTimeout`, não polling) resetado por eventos nativos (mouse/teclado/scroll) **ou** pelo evento customizado `window` `lv:activity` (`ACTIVITY_EVENT` em `src/lib/idle-timeout.ts`) — isso é o que permite qualquer parte da árvore sinalizar atividade sem prop-drilling/Context. Aviso (`AlertDialog`) 60s antes do limite de 10min; no timeout, `signOut()` client-side + `window.location.href` (reload completo, não `router.push`, pra matar qualquer canal Realtime em memória).
- **Vídeo tocando conta como atividade** mesmo sem interação manual: `study-video-player.tsx` escuta `onStateChange` do YouTube IFrame API e, enquanto `PLAYING`, dispara `lv:activity` a cada 25s — sem isso, um aluno assistindo aula sem mexer no mouse seria deslogado no meio do vídeo.
- Sincronização entre abas via `localStorage` (`ACTIVITY_STORAGE_KEY`) + evento `storage`: atividade numa aba reseta o timer das outras, pra uma aba ociosa não derrubar a sessão enquanto o usuário está ativo em outra.
- 10min/60s são constantes fixas em `idle-timeout.ts` (não configurável pelo admin, decisão do usuário) — reduzir temporariamente esses valores é o jeito de testar sem esperar 10 minutos reais.
- Toast "desconectado por inatividade" no login via `?reason=idle` (não confundir com `?error=inactive` do `proxy.ts`, que é conta desativada pelo admin — semântica diferente, e hoje nem é lido pelo `login-form.tsx`).

## Conteúdo rico gerado por membros (não só admin)
- `RichTextEditor` mora em `src/components/ui/rich-text-editor.tsx` (Tiptap) — usado tanto por admins (`lesson-editor.tsx`, `corrida-vendas-manager.tsx`) quanto por membros (`feedback-ticket-form.tsx`).
- Sempre que HTML gerado por MEMBRO for salvo para ser renderizado depois (`dangerouslySetInnerHTML`) por outro usuário (ex: admin vendo um chamado de feedback), sanitize no server action antes de gravar — usar `isomorphic-dompurify` com allowlist explícita (ver `sanitizeRichText` em `src/app/actions/feedback.ts`). Todo `dangerouslySetInnerHTML` anterior no projeto (anúncios, aulas, corridas) só recebe HTML gerado por admins confiáveis — não sanitizado, e deve continuar assim (não é boundary de confiança). Feedback é o primeiro caso de conteúdo rico membro→admin.

## Notificações
- Tabela `notifications`: `id, user_id, type, title, body, link, read_at, created_at`
- Após inserir notificações: chamar `revalidatePath('/dashboard', 'layout')` para invalidar cache do unread count
- Tipos usados (destinatário entre parênteses): `new_training`/`training_replay`/`training_live_expired` (membros do curso), `announcement` (todos os membros), `lesson_published`/`module_published` (membros com acesso ao curso), `task_submitted` (admins), `task_graded` (membro dono da submissão), `task_opened`/`task_closing_soon`/`task_closing_tomorrow` (membros matriculados, via cron `src/app/api/cron/task-reminders/route.ts`), `community_new_post`/`community_new_reply` (admins), `community_reply` (autor do post), `new_feedback` (admins, ao abrir chamado), `new_member_pending` (admins, cadastro aguardando aprovação), `feedback_update` (membro dono do chamado, quando admin muda status ou responde em `src/app/actions/feedback.ts`).
- **Bug real corrigido (v1.66.0): `notifyCourseMembers()` (`src/app/actions/notifications.ts`) nunca notificou ninguém.** A query fazia `.from('member_courses').select('user_id')`, mas a coluna real é `member_id` (migração `012`, e toda outra leitura de `member_courses` no projeto já usava `member_id` corretamente). Coluna inexistente → Postgrest retorna erro → `memberships` vem `undefined` → `if (!memberships?.length) return` sai calado, sem exception. Resultado: `lesson_published`/`module_published` nunca chegaram a ninguém desde que esses tipos existem — nem notificação in-app, nem (agora) e-mail. Corrigido para `select('member_id')`. **O mesmo bug ainda existe** em `src/app/api/cron/task-reminders/route.ts:69` (não corrigido — esse endpoint parece nem estar agendado na VPS hoje, ver nota de e-mail abaixo; ao mexer nos lembretes de tarefa, corrigir esse `user_id` → `member_id` também).
- **Som/toast em tempo real é escopo limitado, não geral**: nem todo tipo acima toca som — só os que têm um componente cliente dedicado assinando Realtime. Hoje existem dois: `src/components/admin/admin-notification-sound.tsx` (admin, tipos `new_feedback`/`new_member_pending`, montado uma vez em `src/app/(admin)/admin/layout.tsx`) e `src/components/members/feedback-notification-sound.tsx` (membro, só tipo `feedback_update`, montado em `src/app/(members)/dashboard/layout.tsx` **só quando `isTester`**, já que só testador abre chamado). O sino do membro (`notification-bell.tsx`) fora desses tipos é só visual — busca ao clicar, sem realtime/som. Ao adicionar um novo tipo que mereça som, decidir explicitamente se cria/estende um desses componentes; não veio de graça por estar na tabela `notifications`.
- **Realtime + polling (híbrido)**: `notifications` está na publication `supabase_realtime` (migração `033`). **Realtime sozinho não é confiável** (confirmado empiricamente: entrega intermitente do evento `postgres_changes`, mesmo com a tabela corretamente na publication e RLS ok) — por isso os dois componentes acima também fazem polling a cada 15s via `getRecentSoundNotifications` (`src/app/actions/notifications.ts`, genérico — recebe a lista de tipos que interessa a cada chamador) como garantia, deduplicando por `id` num `Set` em ref para não tocar duas vezes se ambas as vias pegarem o mesmo evento. Se precisar de realtime em outra tabela, mesma receita de publication: `ALTER PUBLICATION supabase_realtime ADD TABLE x` (idempotente com `DO $$ ... EXCEPTION WHEN duplicate_object`) — mas considere se vale a pena também ter um fallback de polling.
- **Notificações de feedback linkam pro chamado específico, não só pra lista genérica (v1.75.0)**: os 6 `link` de `src/app/actions/feedback.ts` (`new_feedback`, e os 3 pontos que disparam `feedback_update`: atribuição/status/resposta) incluem `?report=<id>` (e `&tab=minhas` quando o destinatário é o dono do chamado, não um admin). `FeedbackPanel` (admin) e `MyFeedbackList` (membro/colaborador) ganharam a prop `initialOpenId` — ao montar, já abrem o card daquele chamado (mesmo fora do filtro de status ativo, graças ao `|| r.id === openId` que já existia em `FeedbackPanel`) e rolam até ele (`scrollIntoView`, via `id="feedback-report-<id>"` em cada card). `admin/feedback/page.tsx` e `dashboard/feedback/page.tsx` só repassam `searchParams.report` como esse `initialOpenId`.
- **Bug real corrigido (v1.75.1): colaborador/admin navegando em `/dashboard` (área do aluno) ainda caía em `/dashboard/feedback` ao clicar numa notificação de chamado.** O rewrite `/dashboard/feedback` → `/admin/feedback` da v1.75.0 só existia no sino do `admin-sidebar.tsx` (prop `isAdmin`, que reflete o **shell atual**, sempre `true` lá) — o sino do `member-sidebar.tsx` nunca passava esse rewrite, então um colaborador (ex.: Raul, área Comercial Nacional) estudando em `/dashboard` no momento em que clicava a notificação ia parar na visão de aluno, mesmo sendo colaborador. Diferente do caso da v1.75.0 (mesma pessoa, sidebar errada), aqui o problema é que "estar no shell admin" e "ser admin/colaborador" são coisas diferentes — um colaborador pode estar navegando em QUALQUER um dos dois shells. Corrigido com uma segunda prop em `notification-bell.tsx`, `redirectFeedbackToAdmin` (default = `isAdmin`, então o sino do admin não precisou mudar), agora calculada a partir do **papel real do usuário** (não do shell): `dashboard/layout.tsx` busca `role` do profile, calcula `isCollaboratorOrAdmin = role !== 'member'` e repassa pro `MemberSidebar` → pras 3 instâncias de `NotificationBell` lá dentro. O rewrite de comunidade (`isAdmin`) continua shell-based, sem mudança — só o de feedback virou role-based, escopo intencionalmente limitado ao que foi pedido.
- **Flakiness observada em teste, não é bug**: em testes automatizados com timeout curto (~2.5s) o clique às vezes "não navegava" — mas isso era só o Next.js/Turbopack compilando a rota `/admin/feedback` pela primeira vez naquela sessão de dev (mesmo padrão de lentidão de primeira-compilação já visto em outras partes do projeto), confirmado ao trocar o timeout fixo por `waitForURL` (a navegação sempre completa, só demora alguns segundos a mais em dev). Não existe em produção (build já compilado) — não é uma condição de corrida real no componente.

## E-mail transacional (Resend)
- `src/lib/email.ts`: wrapper `send()` sobre a lib `resend` (free tier, ~600 e-mails/mês). **No-opa silenciosamente** se `RESEND_API_KEY` não estiver setada (`resend = null`) — nenhum e-mail quebra o fluxo que chamou, erro de envio só vai pro `console.error`. Variáveis: `RESEND_API_KEY`, `NOTIFY_FROM_EMAIL` (remetente), `ADMIN_EMAIL` (destino dos e-mails só-admin) — todas em `.env.example`. **Confirmado em 2026-07-19: `RESEND_API_KEY` não está setada no `.env` da VPS** — hoje NENHUM e-mail deste sistema sai em produção (nem os que já existiam antes deste commit). Pra ativar: criar conta grátis em resend.com, gerar API key, colar no `.env` da VPS e reiniciar o PM2; **também verificar um domínio em resend.com/domains** — sem domínio verificado, o Resend só entrega pro e-mail da própria conta cadastrada, não pros membros de verdade.
- Funções e pontos de disparo (todas fire-and-forget, sem `await` bloqueando a action):
  - `emailAdminNewMemberPending` → `ADMIN_EMAIL`, ao registrar (`auth.ts` `register()`).
  - `emailWelcomeOnRegister` → o próprio cadastrado, mesmo ponto — mensagem de "está em análise", **não** confundir com `emailMemberApproved` (mensagem de "acesso liberado, pode logar", disparada só depois que um admin aprova em `members.ts`).
  - `emailMembersNewAnnouncement` → todos os membros ativos, ao publicar um comunicado (`announcements.ts`).
  - `emailAdminNewFeedback` → `ADMIN_EMAIL`, ao abrir um chamado de feedback.
  - `emailCourseContentPublished` → membros com acesso ao curso, dentro de `notifyCourseMembers()` (`notifications.ts`) — cobre aula E módulo publicados num único ponto (os 5 call sites em `lessons.ts`/`modules.ts` não precisam saber de e-mail).
  - `emailNewTraining` → membros ativos, dentro do `notifyMembers()` local de `training.ts` — cobre treinamento novo e replay.
- `siteUrl` (constante privada em `email.ts`, `NEXT_PUBLIC_SITE_URL` ou fallback `https://universidadelv.com.br`) é prefixado nos links de CTA dos e-mails de conteúdo — os `link`s vindos de `notifyCourseMembers`/`notifyMembers` são paths relativos (`/dashboard/aulas/...`), inúteis num cliente de e-mail sem domínio na frente.
- **Sem toggle de admin por tipo de e-mail** (decisão do usuário: obrigatório por enquanto). Se pedir depois, seguir o padrão dos booleanos já existentes em `src/lib/settings.ts` (ex. `sidebar_training_active`): chave `email_notify_<tipo>`.
- **Textos editáveis pelo admin (v1.67.0)**: assunto/corpo dos 7 e-mails NÃO ficam mais hardcoded em `email.ts` — vêm da tabela `email_templates` (migração `038`, PK `type` = slug tipo `welcome_on_register`), editável em Admin → Configurações → aba "E-mails" (`src/components/admin/email-templates-manager.tsx`). `email.ts` guarda só a lógica: `getTemplate(type)` busca a linha, `renderTemplate(str, vars)` substitui `{{placeholder}}` por regex, `sendTemplate(type, to, vars)` junta os dois e manda. As 7 funções exportadas (`emailWelcomeOnRegister` etc.) mantêm a mesma assinatura de sempre — só montam o objeto `vars` (aplicando os mesmos fallbacks/`escapeHtml`/`\n→<br/>` que já existiam) e chamam `sendTemplate`; **nenhum dos 6 pontos de disparo precisou mudar**. Corpo edita com `RichTextEditor` (Tiptap); lista de variáveis por tipo é um mapa estático `TEMPLATE_META` no próprio manager (só exibição, não persiste). **Cuidado de HTML**: o manager mora dentro do `<form>` gigante de `settings-form.tsx` (aba nova, mesmo componente) — por isso usa `<div>`+`<button type="button">` com `useTransition` em vez de `<form onSubmit>` aninhado (inválido), e o `<Input>` do assunto tem `onKeyDown` com `preventDefault()` no Enter pra não borbulhar e disparar o submit do form de Configurações por engano.

## Presença ("online agora") e tempo na plataforma
- `profiles.last_seen_at` + `profiles.total_time_seconds` (migração `037`) — heartbeat client-side, **não** Supabase Realtime Presence (mesmo motivo do híbrido acima: transporte WebSocket já documentado como não confiável nesta infra).
- `src/components/ui/presence-heartbeat.tsx` grava a cada `PRESENCE_HEARTBEAT_MS` (60s, `src/lib/presence.ts`) via `recordHeartbeat()` (`src/app/actions/presence.ts`), que chama a function Postgres `record_presence_heartbeat` — **um único UPDATE atômico** que soma ao `total_time_seconds` só o intervalo desde o `last_seen_at` anterior, e só se esse intervalo for "contínuo" (≤180s, hardcoded na function — não confundir com a constante JS `PRESENCE_ONLINE_WINDOW_MS`, que é sobre o que conta como "online", não sobre continuidade de sessão). Evita contar como tempo de uso o hiato entre sessões (aba fechada, reaberta dias depois) e evita race condition de duas abas do mesmo usuário.
- Diferente do `IdleLogoutGuard` (só `/dashboard`, detecta atividade real pra decidir deslogar): `PresenceHeartbeat` roda em `/dashboard` **e** `/admin`, sem detectar atividade — só "aba aberta com sessão válida" já conta como online. São features independentes, montadas lado a lado nos dois layouts.
- Leitura no admin: `presenceSinceIso()` (janela de 3min = 3x heartbeat, tolera heartbeats perdidos) filtra quem está online. Card "Online agora" no dashboard (`admin-dashboard-shell.tsx`) quebra por role; bolinha verde na tabela de Membros (`members-table.tsx`). Ambas as telas fazem polling via `<DashboardAutoRefresh />` (`router.refresh()` a cada 30s) — dashboard admin e tabela de Membros eram 100% SSR sem nenhum polling antes disso.
- Tempo total (`formatDuration()`) aparece só em dois lugares, de propósito (decisão do usuário): aba Desempenho do próprio perfil (`profile-tabs.tsx`) e tela de detalhe do membro no admin (`admin/membros/[id]/page.tsx`, onde substituiu uma estimativa falsa `aulas_concluídas × 10min` que existia antes) — **não** é coluna/ranking na tabela geral de Membros.

## Famtours
- Tabela `famtours` (migração `039`): viagens de familiarização divulgadas em Admin → Marketing → aba "Famtours" e, na área do aluno, como seção na home (`/dashboard`) — **card completo** (capa, título/destino, período, descrição, link), sem página própria dedicada (só a seção da home).
- Gestão: admin + colaboradores com a capacidade `famtours` — hoje concedida às áreas **Comercial Nacional** e **Comercial Internacional** (Admin → Membros → Áreas de Colaborador). Mesmo padrão de posse por área de `training_items` (`owner_area_id`, guard `requireContentAccess`/`requireCapability` em `src/app/actions/famtours.ts`).
- **Por que tabela própria e não settings-JSON** (como Corrida/Premiação/PodViajar): essas três são admin-only de propósito; Famtours precisa de escrita por duas áreas de colaborador diferentes, o que settings-JSON não suporta sem sobrescrita entre áreas (last-write-wins no JSON inteiro). **Por que não `marketing_items`**: `capabilityForMarketingCategory()` cai no fallback `'marketing'` para categorias desconhecidas — colaboradores de Marketing (área errada) teriam acesso.
- Sem `order_index`: listagem ordena por `start_date` (viagem mais próxima primeiro) — evita o problema de reorder parcial por colaborador (mesmo motivo do reorder de treinamentos ser admin-only).
- Home do aluno: a seção só aparece se houver famtour `is_active = true` com `end_date` (ou `start_date`, se sem fim) ainda não passado — mesmo padrão "por conteúdo" da Corrida de Vendas, sem toggle de admin. Fica em `src/app/(members)/dashboard/page.tsx`, entre a seção Corrida de Vendas e "Vencedores TamoJunto LV" (posição pedida pelo usuário).

## Grupos
- Tabela `grupos` (migração `040`) — mesmo shape de `famtours` (capa, título, descrição, período, link, `owner_area_id`). Gestão: admin + colaboradores com a capacidade `grupos`, hoje só a área **"Grupos"** (criada sob demanda, sem colaborador nenhum ainda — o admin promove alguém pra ela em Admin → Membros quando quiser).
- **Diferente de Famtours**: não é seção da home — é **item próprio no menu do aluno** (`/dashboard/grupos`, página dedicada com grid de cards) que só aparece quando há grupo ativo e não encerrado. Ver a seção "Menu do aluno" abaixo pro gotcha real que isso expôs.

## Condições Comerciais em cards (substituiu a planilha)
- Tabela `commercial_conditions` (migração `041`) — mesmo padrão de `famtours`, mas **sem datas** (capa, título, descrição, link, `owner_area_id`). Reaproveita a capacidade `comercial` já existente (Comercial Nacional/Internacional) — sem capacidade nova.
- Antes disso, `/dashboard/comercial` (aba "Condições Comerciais", dentro da tela que também hospeda a Corrida de Vendas) mostrava um **iframe de planilha do Google Sheets** vindo de `marketing_items` category='comercial' (`.limit(1)`, só o primeiro item). Isso foi **totalmente substituído** por um grid de cards vindo de `commercial_conditions` — o registro antigo em `marketing_items` (ex. "Hotéis Estrelas") ficou órfão, inofensivo, não é mais lido em lugar nenhum.
- No admin, a sub-aba "Comercial → Condições Comerciais" (antes "Links Comercial", usava `MarketingManager` genérico) agora renderiza `CommercialConditionsManager` dedicado. A sub-aba "Corrida de vendas" (irmã, admin-only) não foi tocada.

## Menu do aluno — `member_nav_order` tem 2 cópias e a do lado renderizado NÃO mescla chaves novas sozinha
- **Bug real (v1.69.0), pego ao adicionar "Grupos" ao menu**: existem duas funções `parseMemberNavOrder` diferentes — uma em `settings-form.tsx` (edição pelo admin, já mesclava chaves ausentes com `ALL_MEMBER_NAV_ITEMS`) e outra em `member-sidebar.tsx` (o que **realmente renderiza** o menu do aluno). A segunda só devolvia o array salvo em `settings.member_nav_order` **como está** — se esse JSON já tinha sido salvo antes (e sempre tinha, é o caso normal), um item de menu novo (`grupos`) nunca aparecia, mesmo com tudo mais implementado certo, até o admin abrir e salvar Configurações de novo manualmente.
- Corrigido: `parseMemberNavOrder` em `member-sidebar.tsx` agora mescla chaves ausentes do `DEFAULT` no fim do array salvo (mesma lógica de `settings-form.tsx`). **Ao adicionar um item de menu novo pro aluno, sempre atualizar as 3 coisas** (não são 2 como no menu admin): `MEMBER_NAV_MAP`/`DEFAULT_NAV_LABELS`/array `DEFAULT` de `parseMemberNavOrder` em `member-sidebar.tsx`; `ALL_MEMBER_NAV_ITEMS`/`DEFAULT_MEMBER_NAV_LABELS_FULL` em `settings-form.tsx`; e a prop de visibilidade condicional em `dashboard/layout.tsx` — e **testar de verdade logado como aluno**, não só olhar o código, porque esse bug não aparece em nenhum erro de TypeScript nem de build.

## Comunidade — ocultar post/resposta (moderação sem exclusão)
- Colunas `is_hidden` (migração `042`) em `community_posts` e `community_replies`. Sem mudança de RLS — a policy `"Admins manage posts/replies" FOR ALL` (migração `011`) já libera `UPDATE` por admin.
- `hidePost`/`hideReply` em `src/app/actions/community.ts` seguem o padrão **do próprio arquivo** (não o `requireAdmin()`+`adminClient` do resto do projeto): `createClient()` de sessão + `.update()` direto, mesmo esquema de `togglePinPost`/`toggleLockPost` — a RLS admin-only é a barreira de segurança real aqui, não um guard explícito na action.
- **Regra de exibição** (decisão do usuário): autor do conteúdo oculto **ainda vê o próprio texto** (com aviso "Ocultado pela moderação"); qualquer outro membro vê só um aviso genérico ("removido pela moderação"), sem o texto real; admin vê tudo sempre, com badge "Oculto" e botão pra reverter. Lógica de mascaramento é só de **render** (`isMaskedForViewer = is_hidden && !isAdmin && !isAuthor`), não filtro de RLS/SELECT — o dado real sempre chega ao client, só não é mostrado.
- `community-posts.tsx` (lista) e `community-post-view.tsx` (detalhe do post + cada `ReplyItem`) implementam a mesma lógica separadamente — são componentes React distintos, sem componente de card compartilhado entre post e reply.

## Upload de imagem de capa (padrão `uploadXCover`) — cuidado com foto de celular
- Padrão usado em `famtours-manager.tsx`/`trainings-manager.tsx` (e potencialmente outros managers com capa): o `<input type="file">` guarda o `File` cru no state; o upload/compressão (`toWebP` em `src/lib/image.ts`, via `sharp`) só acontece **depois**, dentro da server action (`uploadFamtourCover`/`uploadTrainingCover`), chamada como Server Action do Next.js. Isso significa que o **arquivo original (não comprimido)** é quem precisa caber no limite de `experimental.serverActions.bodySizeLimit` (`next.config.ts`, hoje `10mb` — era `5mb`, subido na v1.68.1).
- **Bug real (v1.68.1)**: uma foto de celular direto da câmera (facilmente 5–15MB) estourava esse limite, e como não havia `try/catch` em volta do `await uploadXCover(file)`, o erro do Next.js (corpo grande demais) não virava um toast — quebrava a página inteira (`This page couldn't load`) sem log útil no servidor. Corrigido com duas camadas: (1) guard no `handleFileChange` recusando arquivo `> 8MB` na hora da seleção, com toast explicando; (2) `try/catch` em volta do bloco inteiro do `startTransition` do submit, com toast de fallback genérico. **Ao criar um novo manager com upload de capa, replicar as duas camadas** — não é só copiar o padrão de `cover_url`/preview.

## Dashboard admin — aba "Gráficos" (Recharts, filtro de curso e data)

- **Desde v1.76.0**, a aba "Gráficos" do Dashboard (`/admin`, `src/components/admin/dashboard-charts.tsx`) usa gráficos de verdade (`recharts`, já dependência do projeto — mesmo template visual de `relatorios-marketing.tsx`: `AreaChart`/`LineChart`/`BarChart` horizontal/`PieChart` donut, tooltip com hex cru porque CSS var não resolve em inline style do Recharts) em vez das barrinhas CSS antigas (`HBar`). Tem filtro de **curso** (`<select>`) e de **data** (atalhos 7/30/90 dias/Este ano/Tudo + período customizado de/até, mesmo padrão visual da aba Atividades de Relatórios).
- **Tudo client-side, sem round-trip ao servidor por troca de filtro** — `src/app/(admin)/admin/page.tsx` busca os dados crus uma vez (estendeu 3 queries já existentes com colunas novas: `member_progress.completed_at`, `modules.course_id`, `profiles.created_at`; mais 2 queries novas: `courses` e `member_courses`) e `dashboard-charts.tsx` recomputa tudo via `useMemo` a cada troca de filtro — mesma filosofia de `relatorios-marketing.tsx`, evita o problema já documentado no projeto de "sem live update sem reload".
- **Regra de escopo (dois eixos independentes, decisão do usuário)**: o filtro de **curso** decide **quem** entra no cálculo (`relevantModules`/`relevantLessonIds`/`relevantMemberIds` — quando um curso é selecionado, membros ficam restritos a quem está matriculado nele via `member_courses`, não todos os membros ativos); o filtro de **data** decide **quando** (só afeta os gráficos com timestamp: "Conclusões ao Longo do Tempo", "Aulas Mais Concluídas", "Novos Membros ao Longo do Tempo", e os 2 KPIs correspondentes). **"Progresso por Módulo" e "Distribuição de Engajamento" nunca são afetados pela data** — são "estado atual", igual sempre foram. "Novos Membros ao Longo do Tempo" nunca é afetado pelo curso — é crescimento da plataforma como um todo, não daquele curso específico.
- Bucketização das séries temporais é automática pelo tamanho do intervalo (`pickGranularity` em `dashboard-charts.tsx`): ≤31 dias vira barras/pontos por dia, 32–180 dias por semana, acima disso por mês — evita um gráfico de "Tudo" (podendo ser anos) com centenas de pontos diários ilegíveis.
- Abas "Visão Geral" e "Insights" do mesmo Dashboard **não foram tocadas** — continuam sem filtro de data/curso, mostrando sempre "desde sempre, todos os cursos" (o pedido do usuário foi especificamente sobre a aba Gráficos).
- Mesma visibilidade de sempre pra colaborador: sem filtro por área (decisão já documentada — "Dashboard mostra as mesmas métricas pra admin e colaborador"), então o `<select>` de curso mostra **todos os cursos da plataforma** pra qualquer colaborador, não só os da própria área.

## Convenções
- Server actions ficam em `src/app/actions/`
- Componentes admin em `src/components/admin/`
- Componentes UI reutilizáveis em `src/components/ui/`
- `settings` são pares chave-valor na tabela `site_settings`; tipo e defaults em `src/lib/settings.ts`
- **Bug real corrigido (v1.84.2): favicon customizado (`settings.favicon_url`) competia com um `src/app/favicon.ico` estático** (ícone padrão do `create-next-app`, nunca substituído desde o commit inicial). `layout.tsx` já injetava o favicon certo via `icons: { icon: settings.favicon_url }`, mas o Next.js App Router SEMPRE adiciona o favicon de convenção de arquivo junto, resultando em **dois** `<link rel="icon">` na página — qual dos dois o navegador realmente usa varia por navegador/plataforma (mobile costuma ser mais inconsistente nessa escolha que desktop), explicando favicon errado aparecer só em alguns ambientes. Corrigido removendo `src/app/favicon.ico` — sobra só o customizado. Sem `apple-touch-icon` configurado (ícone de tela inicial no iOS é diferente do favicon de aba) — não pedido, fica de fora de propósito.
- **Cor de destaque separada por ambiente (v1.81.0)**: `primary_color` (admin, global) e `member_accent_color` (só `/dashboard`, default `'green'`) são 2 settings independentes, mesmos 7 presets de `COLOR_PRESETS`. `getColorStyleTag()` injeta `:root`/`.dark` uma vez no `layout.tsx` raiz (afeta tudo); `getMemberColorStyleTag()` (mesma função, mesmos tokens: `--primary`/`--ring`/`--sidebar-primary`) gera as regras escopadas em `.member-theme`/`.dark .member-theme` em vez de `:root`/`.dark` — injetado só dentro de `dashboard/layout.tsx` (a raiz do App Router não permite `<head>` por layout aninhado, então o `<style>` vai solto no `<body>`, válido em HTML5). `.member-theme` é a classe no `<div>` raiz do layout do membro — como quase toda UI (botões, nav ativo, badges) já usa os tokens `primary`/`--primary`, a mudança propaga sozinha, sem tocar componente por componente. Fundos de página (`--background`/`--card`) não são tocados por nenhum dos dois — só os tokens de destaque. Editável em Admin → Configurações → "Cor da Área do Membro" (clone do bloco "Cor principal", grava a chave nova via `updateSettings`).
- **Sidebar do membro 100% verde sólida (v1.85.0)**: escalada do item acima — o pedido original ("mais verde") virou, a pedido explícito do usuário relayando feedback do diretor, o fundo INTEIRO da sidebar/menu (`src/components/members/member-sidebar.tsx`) em `bg-primary` sólido (antes era `bg-card` neutro com só os itens ativos tingidos de verde a 10% de opacidade). Isso exigiu recolorir TODO texto/ícone/borda do componente para variantes de branco (`text-white`, `text-white/70`, `border-white/25`, `hover:bg-white/10`, etc.) — os tokens neutros de sempre (`text-muted-foreground`, `text-foreground`) ficam ilegíveis sobre um fundo `bg-primary` sólido. Cobre: linha da marca/logo, botão de busca rápida, todos os itens de nav (ativo e inativo), botão "Minimizar", rodapé de usuário (avatar/nome/e-mail/Sair/versão), e — fora do `SidebarContent` — o `<header>` mobile, o `<aside>` do drawer mobile e o `<aside>` desktop (esses 3 também tinham `bg-card`/borda próprios, substituídos por `bg-primary` sem borda). **Restrição mantida**: fundo das PÁGINAS (`--background`/`--card`) continua branco/preto conforme tema — só a sidebar virou sólida, não o app inteiro (mesma restrição original: "não precisa tirar o fundo das páginas do branco ou preto"). `MemberFeedbackWidget` foi deixado intencionalmente com sua cor violeta original (regra própria de "cor reconhecível entre ambientes" já documentada) — segue legível contra o verde.
- **`NotificationBell` ganhou `triggerClassName?: string`** (`src/components/members/notification-bell.tsx`) — sobrescreve só as classes do botão-gatilho (não do painel dropdown, que continua neutro/`bg-popover`), via `cn(base, open && ..., triggerClassName)`. Necessário porque o sino é montado tanto no rodapé/header da sidebar (agora verde) quanto potencialmente em fundos neutros — sem essa prop, o ícone `text-muted-foreground` desapareceria contra `bg-primary` sólido. Mesmo padrão aplicado ao `<ThemeToggle className="...">` já existente, reaproveitado (não é prop nova).
- Nav do admin é duplicado em dois lugares que precisam ficar em sincronia: `NAV_ITEMS` em `src/components/admin/admin-sidebar.tsx` (render real, com ícone) e `ALL_NAV_ITEMS` em `src/components/admin/settings-form.tsx` (aba "Menu Admin", reordenável via `nav_order`). Ao adicionar página admin nova, adicionar nos dois + no default de `nav_order` em `src/lib/settings.ts`.
- **Perfil (admin e membro) é a MESMA tela**: `src/app/(admin)/admin/perfil/page.tsx` e `src/app/(members)/dashboard/perfil/page.tsx` renderizam o mesmo `<ProfileTabs>` (`src/components/members/profile-tabs.tsx`), cuja aba "Meu Perfil" usa `ProfileFormCompact` (`src/components/members/profile-form-compact.tsx`) — único lugar a editar para mudar campos de perfil de qualquer um dos dois papéis. Campos hoje: nome, email (read-only), avatar, empresa (`company`), cargo (`job_title`), LinkedIn (`linkedin_url`), salvos via `updateProfile` em `src/app/actions/profile.ts`. Existe também `src/components/members/profile-form.tsx`, mas é código morto (nenhuma página importa) — não editar por engano achando que é o formulário real.

## Instrutor com perfil vinculado (v1.82.0)

- **Migração `047`**: `profiles.bio TEXT NOT NULL DEFAULT ''` (rich text HTML, sem sanitização — mesma classe de confiança de conteúdo colaborador/admin→membro já documentada acima) + `courses.instructor_profile_id UUID REFERENCES profiles(id) ON DELETE SET NULL` (nullable — curso sem vínculo continua 100% nos 3 campos de texto livre de sempre).
- **`course-editor.tsx`**: radio "Vincular a um colaborador do sistema" vs "Preencher manualmente" (`instructorMode`, inicializado por `course.instructor_profile_id ? 'linked' : 'manual'`). Modo vinculado mostra um `<select>` de candidatos (`instructorCandidates`, prop vinda do server — `profiles` com `role IN ('admin','collaborator')` e `active=true`, buscado em `admin/cursos/[id]/page.tsx` junto com curso/módulos); modo manual mostra os 3 campos de sempre (nome/cargo/foto). Ao salvar, sempre manda os 3 campos manuais **e** `instructor_profile_id` (vazio quando modo manual) — `updateCourse` (`courses.ts`) grava os dois, sem apagar um quando o outro é usado (permite voltar pro modo manual sem perder o que já tinha).
- **Exibição pro aluno** (`dashboard/cursos/[id]/page.tsx`): quando `course.instructor_profile_id` existe, busca o perfil vinculado (nome/cargo/foto/bio) — **precisa de `createAdminClient()`**, não o client de sessão do membro (RLS `profiles_select_own` bloquearia silenciosamente a leitura do perfil de outra pessoa, mesmo gotcha já documentado). Cada campo tem fallback independente pro texto livre do curso (`linkedInstructor?.avatar_url || course.instructor_photo_url`, etc.) — permite trocar só o nome no perfil e manter a foto manual antiga, por exemplo. Bio só aparece quando o perfil vinculado tiver uma (perfil sem bio preenchida = sem essa seção, sem quebrar o card).
- **Edição da bio**: `ProfileFormCompact` (`bio`/`showBio` novos, opcionais) — `showBio` é `true` sempre em `/admin/perfil`, e `profile?.role !== 'member'` em `/dashboard/perfil` (colaborador estudando como membro ainda vê o campo; membro comum não, já que bio só faz sentido pra quem pode ser instrutor). `EditMemberDialog` ganha o mesmo campo (só quando `selectedRole !== 'member'`), permitindo o admin editar a bio de qualquer colaborador — usa `updateMember`'s `data.bio` (opcional, só grava quando enviado: `...(data.bio !== undefined ? {bio: data.bio} : {})`, pra não apagar a bio de alguém sem querer ao editar outros campos por outro caminho).

## Rollout faseado (em andamento, deadline 31/08/2026)
- Plano completo em `C:\Users\thiago.leal\.claude\plans\recursive-painting-wave.md`. Fase 1 (Marketing/T.I.) em execução.
- Testadores são marcados com a tag **"Beta"** (criada em Admin → Membros → Gerenciar tags, atribuída via `assignMemberTags` já existente).
- Feedback virou um "chamado" completo, não mais popup: item "Feedback" na sidebar do membro (`src/components/members/member-feedback-widget.tsx`, só aparece com a tag Beta — checado em `src/app/(members)/dashboard/layout.tsx` e de novo, server-side, na própria página `src/app/(members)/dashboard/feedback/page.tsx` — bloqueia acesso direto por URL) leva para uma página com formulário completo (`feedback-ticket-form.tsx`: título, tipo, editor rico com imagem inline, link, anexos de foto) + lista "Minhas solicitações" (`my-feedback-list.tsx`).
- Dados em `feedback_reports` (migração `031`) + `title`/`link_url` (migração `032`) + `feedback_attachments` (tabela filha, bucket `feedback-attachments`). Visível em Admin → Feedback (`src/app/(admin)/admin/feedback/`).
- **Timeline, responsável e status de 3 estados (migração `034`)**: `feedback_reports.status` agora é `'open' | 'in_progress' | 'resolved'` e tem `assigned_to` (FK para `profiles`, admin responsável). Cada evento (criação, atribuição, mudança de status, resposta) vira uma linha em `feedback_events`, com `actor_name`/`assigned_name` **denormalizados como texto** no momento do evento (não joins) — evita de propósito o gotcha de N→1 acima e mantém o histórico estável mesmo se o admin for renomeado/removido depois. Não existe mais `admin_note` (campo único que se sobrescrevia) — virou `addFeedbackNote`, que só insere um evento `note_added`, nunca edita/apaga os anteriores. `FeedbackTimeline` (`src/components/ui/feedback-timeline.tsx`) e `ImageLightbox` (`src/components/ui/image-lightbox.tsx`, visualizador inline de anexos sem abrir nova aba) são compartilhados entre o painel admin (`feedback-panel.tsx`) e a lista do membro (`my-feedback-list.tsx`) — membro vê a timeline completa, não só a última resposta.
- No painel admin, o card expandido de um chamado fica visível mesmo se o status mudar para fora da aba filtrada (`r.id === openId` no filtro de `feedback-panel.tsx`) — sem isso, o card some da tela no meio da edição assim que o `router.refresh()` do `updateFeedbackStatus` chega.
- **Cores distintas por status (v1.78.1)**: `STATUS_BADGE_VARIANT` (variantes `default`/`secondary`/`outline` do `Badge`) foi trocado por `STATUS_BADGE_CLASS` (classes de cor explícitas: aberto=azul, em andamento=âmbar, finalizado=verde) em `feedback-panel.tsx` e `my-feedback-list.tsx` — as 3 variantes shadcn não geravam contraste visual suficiente entre si.
- **`addFeedbackNote` (`src/app/actions/feedback.ts`) é bidirecional**: tanto admin quanto o membro dono do chamado podem chamá-la para responder (mesmo botão "Enviar resposta" nos dois lados, usando `RichTextEditor` para negrito/itálico/lista/etc — texto sanitizado com o mesmo `SANITIZE_CONFIG` da mensagem original, já que agora é conteúdo de duas origens diferentes renderizado pro outro lado). Autorização: admin sempre pode; membro só no próprio chamado (`actor.id === report.user_id`), checado no server (não só escondido na UI). Notificação vai para "quem não escreveu": admin responde → avisa o membro; membro responde → avisa o `assigned_to` se houver, senão todos os admins (`notifyAllAdmins`). `assignFeedback`/`updateFeedbackStatus` são admin-only via `requireAdmin()` — antes dessa checagem, a RLS bloqueava o UPDATE de qualquer não-admin silenciosamente (0 linhas, sem erro), mas o código seguia inserindo evento/notificação como se tivesse funcionado.
- `admin-notification-sound.tsx` também tocava som só para `new_feedback`/`new_member_pending` — agora inclui `feedback_update` também, pois esse tipo passou a cobrir "membro respondeu"/"chamado atribuído a mim", que o admin quer ouvir na hora.

## Notion — documentação viva do projeto (MANTER ATUALIZADO)

O projeto tem uma página no Notion que funciona como documentação central para a equipe (não-devs incluídos). **Sempre que concluir um conjunto de mudanças, atualize o Notion junto** — mesma regra do commit/push, é parte do "terminar a tarefa". Acesso via conector "claude.ai Notion" (ferramentas `mcp__claude_ai_Notion__*`; se não aparecerem, buscar com ToolSearch por "notion").

- **Página raiz "Universidade LV"**: ID `39f6c40ae40880ca892de170cda199c2`. Subpáginas: Visão Geral, Arquitetura & Stack, Funcionalidades, Histórico de Versões, Roadmap de Rollout.
- **O que atualizar quando**:
  - **Todo bump de versão** → adicionar linha na tabela de "Histórico de Versões" (versão, data, descrição em linguagem de negócio, não técnica).
  - **Feature nova ou mudança de stack/infra** → refletir em "Funcionalidades" e/ou "Arquitetura & Stack".
  - **Mudança de datas/fases do rollout** → base "Cronograma de Rollout" (data source `28d46862-2b2c-49f3-bb28-b442d0d2bb26`, dentro da página Roadmap; tem views de tabela/timeline/calendário). Datas das fases intermediárias estão como "Proposta — confirmar datas"; só 31/08/2026 é fixa.
  - **Chamados de feedback** → base "Chamados" (data source `cae77ba3-60af-4e67-ae31-e81cb7c2f362`): espelho de `feedback_reports`/`feedback_events` (a coluna de ligação dos events é `feedback_id`, não `report_id`). **Desde v1.73.0, isso é automático** (ver seção "Sincronização automática de chamados com o Notion" abaixo) — não é mais algo que eu atualizo manualmente. Formato por chamado: propriedades na tabela (Título/Tipo/Status/Aberto por/Responsável/Data) + conteúdo da página com "Mensagem original" (autor, data/hora em BRT = UTC−3), "Timeline" (tabela com todos os eventos: abertura, atribuição, mudanças de status, respostas dos dois lados) e "Resolução" (parágrafo estático, não atualizado automaticamente — ver seção abaixo).
- Escrever sempre em pt-BR, tom de documentação para equipe (o Notion é lido por gente de negócio, não só devs).
- O `.mcp.json` na raiz do repo (servidor `notion` via mcp.notion.com) acabou não sendo a via usada — a conexão real vem do conector da conta claude.ai do usuário. O arquivo ficou no repo por ser inócuo (e útil para quem usar o CLI standalone). **Essa conexão MCP só existe dentro de uma conversa comigo — o servidor Next.js da VPS não tem acesso a ela.** Para automação real a partir do próprio app (ver abaixo), foi criada uma integração Notion separada, com token próprio.

## Sincronização automática de chamados com o Notion (`src/lib/notion.ts`)

- **Desde v1.73.0**, toda mutação de um chamado de feedback (`src/app/actions/feedback.ts`) espelha automaticamente na base "Chamados" do Notion — não depende mais de eu ser acionado. Mecanismo independente do conector MCP: usa a API REST do Notion direto via `@notionhq/client`, autenticado com uma **Internal Integration** própria do workspace (token em `NOTION_API_KEY`, chamada "Universidade LV" no Notion) — precisa estar compartilhada com a página "Chamados" (ou a página-mãe "Universidade LV") via "..." → Conectores, senão a API retorna 404 mesmo com o token certo.
- Env vars opcionais (`NOTION_API_KEY`, `NOTION_CHAMADOS_DATA_SOURCE_ID`) — mesmo padrão de no-op silencioso do `RESEND_API_KEY`/`email.ts`: sem elas, `notion.ts` não lança erro, só não sincroniza. **Confirmar que ambas estão setadas no `.env` da VPS** (além do `.env.local` local) — sem isso, o sync fica no-op em produção mesmo funcionando local.
- **`feedback_reports.notion_page_id`** (migração `044`) guarda o id da página do Notion criada para cada chamado — é o que permite os eventos seguintes (atribuição/status/resposta) atualizarem a MESMA página em vez de criar uma duplicata. Os 4 pontos de mutação em `feedback.ts` (cada um já insere uma linha em `feedback_events`) chamam o Notion no mesmo lugar, sempre fire-and-forget (sem `await` bloqueante): `submitFeedback` cria a página e grava o `notion_page_id` de volta; `assignFeedback`/`updateFeedbackStatus`/`addFeedbackNote` anexam uma linha na Timeline e atualizam a propriedade correspondente (Responsável/Status).
- **Como a Timeline é atualizada sem manipular blocks do Notion**: em vez de achar o `block_id` da tabela nativa via API de blocks, `notionAppendTimelineRow` usa o endpoint `pages.updateMarkdown` (comando `update_content`, busca-e-substitui) procurando a única ocorrência de `</table>` na página e inserindo a linha nova antes dela — funciona porque cada chamado tem exatamente uma tabela Timeline na página.
- **Rótulos de Status do Notion ≠ rótulos do app**: o Select "Status" na base usa `Aberto/Em andamento/Resolvido`; o `STATUS_LABEL` interno do app (usado em toasts/UI) usa `Finalizado` em vez de `Resolvido` para o mesmo `status: 'resolved'` — `notion.ts` tem seu próprio mapa, não reaproveita o do app.
- **"Resolução" não é reescrita automaticamente** (decisão deliberada, não lacuna) — fica como placeholder estático criado junto com a página ("Em aberto, sem resolução ainda."). A Timeline (atualizada a cada evento) já é a fonte viva de verdade; editar aquele parágrafo de prosa via API exigiria achar o block certo por uma chamada extra, para um ganho pequeno.
- **Chamados criados antes da v1.73.0 foram sincronizados manualmente e depois tiveram o `notion_page_id` preenchido via backfill** (script único, não faz parte do fluxo de código) — qualquer chamado novo daqui pra frente já nasce com o `notion_page_id` certo desde a criação, sem precisar de backfill.

## Log de Atividades Administrativas (Admin → Relatórios → aba "Atividades")

- Tabela `admin_activity_log` (migração `043`) — log transversal (sem FK de uma entidade só, ao contrário de `feedback_events`): `actor_id` (nullable, `ON DELETE SET NULL`) + `actor_name`/`actor_role` denormalizados, `action` (`create|update|delete|toggle|upload|reorder`, vocabulário fechado por CHECK), `entity_type` (string livre, vocabulário fechado em código via `ACTIVITY_ENTITY_LABELS`), `entity_id` nullable sem FK (não dá pra referenciar ~20 tabelas de uma coluna só — é log, não dado transacional), `entity_label` denormalizado, `detail` texto livre. RLS só-admin-lê, sem policy de INSERT (só `adminClient`, mesmo padrão de `feedback_events`/`notifications`).
- **Helper central `src/lib/activity-log.ts`**: `logActivity(ctx: AdminContext, {action, entityType, entityLabel, entityId?, detail?})` — busca `full_name` do ator (1 query extra por chamada, sem cache entre invocações) e insere a linha via `adminClient`. **Sempre chamado sem `await` (fire-and-forget)** nos ~90 pontos de instrumentação, com `try/catch` interno como segunda camada — o log nunca pode quebrar nem atrasar a mutação principal. `diffFields(before, after, fieldLabels)` compara um objeto "antes" (buscado do banco pré-update) com o payload novo e devolve só os *nomes* dos campos que mudaram (não os valores antigo/novo — decisão do usuário: resumo, não diff completo, para manter viável em ~90 pontos).
- **Cobertura**: TODAS as ~90 funções de mutação (create/update/delete/toggle/upload/reorder) nos ~20 arquivos de `src/app/actions/*.ts` chamam `logActivity`. Padrão por tipo de ação: create → loga após o insert (com `entityId` se o insert já retornava `id`, senão `.select('id')` foi adicionado); update → busca "antes" (reaproveitando um select que já existisse por outro motivo quando possível, ex. `updateTrainingItem` já buscava `prev` pra decidir se notifica) e usa `diffFields`; upload → `detail` descreve o arquivo; toggle → `detail` descreve o novo estado (`'ativou'`/`'publicou'`/etc); delete → busca o label ANTES de excluir (senão a linha já não existe mais pra consultar).
- **Exceções ao padrão**: `updateSettings` (`settings.ts`, ~75 chaves em `site_settings`) loga genérico sem lista de campos — diff granular seria impraticável e pouco útil de ler. `reorder*` não faz diff de campo (`detail` opcional tipo "moveu para cima"). `feedback.ts` tem seu próprio `requireAdmin()` local (retorna só `{userId}`, não o `AdminContext` completo de `authz.ts`) — usa um helper interno `toAdminContext(userId)` pra montar um `AdminContext` mínimo só pra poder chamar `logActivity`; `addFeedbackNote` só loga quando quem respondeu é admin (resposta do próprio membro no seu chamado não é "atividade administrativa", já fica registrada em `feedback_events`).
- **Guards de autorização corrigidos como pré-requisito** (v1.70.0): `certificates.ts` (issueCertificate/approveCertificate/revokeCertificate/updateCertificateSettings/uploadCertificateTemplate/saveCertificateNamePosition) não tinha NENHUM guard antes — qualquer usuário autenticado conseguia emitir/revogar certificado; ganhou `requireAdmin()` + trocou pra `createAdminClient()`. `togglePinPost`/`toggleLockPost`/`hidePost`/`hideReply`/`deletePost` (`community.ts`) também ganharam `requireAdmin()` (antes só a RLS + a UI escondendo os botões protegiam). **`deleteReply` ficou de fora de propósito**: o próprio autor (membro comum) também pode excluir a própria resposta (`canDelete = isAdmin || isAuthor` na UI) — `requireAdmin()` ali quebraria esse fluxo. Consequência: exclusão de resposta de terceiro feita por admin não aparece no log de atividades nesta v1 (lacuna consciente, não bug).
- **UI**: `src/components/admin/atividades-log.tsx` (server component) — lista paginada (offset simples, `range()` + `count:'exact'`) com filtros por querystring (`?tab=atividades&ator=&entidade=&acao=&de=&ate=&page=`, `<form method="get">`, mesmo padrão de tabs por `?tab=` já usado em Relatórios). Herda a proteção admin-only da própria página (`requireAdminPage()` em `relatorios/page.tsx`) — colaborador nunca vê a aba, mas suas ações continuam sendo logadas (`actor_role: 'collaborator'`, badge "Colaborador" na listagem).

## Atualização deste arquivo
Mantenha este CLAUDE.md atualizado com decisões arquiteturais, convenções novas ou contexto relevante descoberto durante o desenvolvimento. Não documente o óbvio — apenas o que um novo Claude não conseguiria derivar lendo o código.
